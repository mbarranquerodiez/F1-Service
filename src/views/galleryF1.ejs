<!doctype html>
<html lang="en" data-bs-theme="dark">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="description" content="" />
  <meta
    name="author"
    content="Mark Otto, Jacob Thornton, and Bootstrap contributors"
  />
  <meta name="generator" content="Astro v5.13.2" />
  <title>Galería de Fórmula 1</title>

  <!-- Bootstrap 5.3 desde CDN -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
    crossorigin="anonymous"
  />

  <!-- Fuente Playfair Display -->
  <link
    href="https://fonts.googleapis.com/css?family=Playfair+Display:700,900&display=swap"
    rel="stylesheet"
  />

  <!-- Tu hoja de estilos personalizada -->
  <link href="/src/css/allRaces.css" rel="stylesheet" />

  <meta name="theme-color" content="#712cf9" />

  <style>
    .bd-placeholder-img {
      font-size: 1.125rem;
      text-anchor: middle;
      -webkit-user-select: none;
      -moz-user-select: none;
      user-select: none;
    }
    @media (min-width: 768px) {
      .bd-placeholder-img-lg {
        font-size: 3.5rem;
      }
    }
    .b-example-divider {
      width: 100%;
      height: 3rem;
      background-color: #0000001a;
      border: solid rgba(0, 0, 0, 0.15);
      border-width: 1px 0;
      box-shadow: inset 0 0.5em 1.5em #0000001a,
        inset 0 0.125em 0.5em #00000026;
    }
    .b-example-vr {
      flex-shrink: 0;
      width: 1.5rem;
      height: 100vh;
    }
    .bi {
      vertical-align: -0.125em;
      fill: currentColor;
    }
    .nav-scroller {
      position: relative;
      z-index: 2;
      height: 2.75rem;
      overflow-y: hidden;
    }
    .nav-scroller .nav {
      display: flex;
      flex-wrap: nowrap;
      padding-bottom: 1rem;
      margin-top: -1px;
      overflow-x: auto;
      text-align: center;
      white-space: nowrap;
      -webkit-overflow-scrolling: touch;
    }
    .btn-bd-primary {
      --bd-violet-bg: #712cf9;
      --bd-violet-rgb: 112.520718, 44.062154, 249.437846;
      --bs-btn-font-weight: 600;
      --bs-btn-color: var(--bs-white);
      --bs-btn-bg: var(--bd-violet-bg);
      --bs-btn-border-color: var(--bd-violet-bg);
      --bs-btn-hover-color: var(--bs-white);
      --bs-btn-hover-bg: #6528e0;
      --bs-btn-hover-border-color: #6528e0;
      --bs-btn-focus-shadow-rgb: var(--bd-violet-rgb);
      --bs-btn-active-color: var(--bs-btn-hover-color);
      --bs-btn-active-bg: #5a23c8;
      --bs-btn-active-border-color: #5a23c8;
    }
    .bd-mode-toggle {
      z-index: 1500;
    }
    .bd-mode-toggle .bi {
      width: 1em;
      height: 1em;
    }
    .bd-mode-toggle .dropdown-menu .active .bi {
      display: block !important;
    }
  </style>
</head>

<body>
  <svg xmlns="http://www.w3.org/2000/svg" class="d-none">
    <symbol
      id="aperture"
      fill="none"
      stroke="currentColor"
      stroke-linecap="round"
      stroke-linejoin="round"
      stroke-width="2"
      viewBox="0 0 24 24"
    >
      <circle cx="12" cy="12" r="10"></circle>
      <path
        d="M14.31 8l5.74 9.94M9.69 8h11.48M7.38 12l5.74-9.94M9.69 16L3.95 6.06M14.31 16H2.83m13.79-4l-5.74 9.94"
      ></path>
    </symbol>
    <symbol id="cart" viewBox="0 0 16 16">
      <path
        d="M0 1.5A.5.5 0 0 1 .5 1H2a.5.5 0 0 1 .485.379L2.89 3H14.5a.5.5 0 0 1 .49.598l-1 5a.5.5 0 0 1-.465.401l-9.397.472L4.415 11H13a.5.5 0 0 1 0 1H4a.5.5 0 0 1-.491-.408L2.01 3.607 1.61 2H.5a.5.5 0 0 1-.5-.5zM3.102 4l.84 4.479 9.144-.459L13.89 4H3.102zM5 12a2 2 0 1 0 0 4 2 2 0 0 0 0-4zm7 0a2 2 0 1 0 0 4 2 2 0 0 0 0-4zm-7 1a1 1 0 1 1 0 2 1 1 0 0 1 0-2zm7 0a1 1 0 1 1 0 2 1 1 0 0 1 0-2z"
      ></path>
    </symbol>
    <symbol id="chevron-right" viewBox="0 0 16 16">
      <path
        fill-rule="evenodd"
        d="M4.646 1.646a.5.5 0 0 1 .708 0l6 6a.5.5 0 0 1 0 .708l-6 6a.5.5 0 0 1-.708-.708L10.293 8 4.646 2.354a.5.5 0 0 1 0-.708z"
      ></path>
    </symbol>
  </svg>
   <div class="container-fluid">
<header class="border-bottom lh-1 py-3">
  <div class="row flex-nowrap justify-content-center align-items-center">
    <div class="col-4 text-center">
      <a class="blog-header-logo text-decoration-none" href="/home">
        <img src="/src/public/LogoInfo1.png" alt="Info1" style="max-height: 100px; width: auto;"> <!-- Ajusta el tamaño con style si es necesario -->
      </a>
    </div>
  </div>
</header>
<div class="nav-scroller py-1 mb-3 border-bottom">
<nav class="nav nav-underline justify-content-between">
<a class="nav-item nav-link link-body-emphasis" href="/home">Home</a>
<a class="nav-item nav-link link-body-emphasis" href="/getRacesByYear">Carreras</a>
<a class="nav-item nav-link link-body-emphasis" href="/getDriversByYear">Pilotos</a>
<a class="nav-item nav-link link-body-emphasis" href="/circuits">Circuitos</a>
<a class="nav-item nav-link link-body-emphasis active" href="/gallery">Galería</a>
</nav>
</div>
</div>
  <main class="container-fluid">
    <div class="p-4 p-md-5 mb-4 rounded text-body-emphasis bg-dark">
      <div class="row justify-content-center">
        <div class="col-lg-12 text-center">
          <h1 class="display-4 fst-italic">Galería de Fórmula 1</h1>
          <p class="lead my-3">Explora fotografías de la temporada de Fórmula 1.</p>
        </div>
      </div>
      <div class="row gallery-info">
        <div class="col-md-12">
          <!-- Filters Section -->
          <section class="filters card bg-secondary text-light mb-4">
            <div class="card-body">
              <h2 class="card-title">Filtros</h2>
              <form id="galleryFilterForm" method="GET" action="/gallery">
                <div class="row">
                  <div class="col-md-4 mb-3">
                    <label for="yearSelect" class="form-label">Año</label>
                    <select id="yearSelect" name="year" class="form-select">
                      <% const currentYear = typeof selectedYear !== 'undefined' ? selectedYear : '2025'; %>
                      <% for(let y = 1975; y <= new Date().getFullYear(); y++) { %>
                        <option value="<%= y %>" <% if(y == currentYear) { %>selected<% } %>><%= y %></option>
                      <% } %>
                    </select>
                  </div>
                  <div class="col-md-4 mb-3">
                    <label for="teamSelect" class="form-label">Equipo/Escudería</label>
                    <select id="teamSelect" name="team" class="form-select">
                      <% const currentTeam = typeof selectedTeam !== 'undefined' ? selectedTeam : ''; %>
                      <option value="" <% if(currentTeam === '') { %>selected<% } %>>Todas</option>
                      <option value="Mercedes" <% if(currentTeam === 'Mercedes') { %>selected<% } %>>Mercedes</option>
                      <option value="Red Bull Racing" <% if(currentTeam === 'Red Bull Racing') { %>selected<% } %>>Red Bull Racing</option>
                      <option value="Ferrari" <% if(currentTeam === 'Ferrari') { %>selected<% } %>>Ferrari</option>
                      <option value="McLaren" <% if(currentTeam === 'McLaren') { %>selected<% } %>>McLaren</option>
                      <option value="Aston Martin" <% if(currentTeam === 'Aston Martin') { %>selected<% } %>>Aston Martin</option>
                      <option value="Alpine" <% if(currentTeam === 'Alpine') { %>selected<% } %>>Alpine</option>
                      <option value="Williams" <% if(currentTeam === 'Williams') { %>selected<% } %>>Williams</option>
                      <option value="AlphaTauri" <% if(currentTeam === 'AlphaTauri') { %>selected<% } %>>AlphaTauri</option>
                      <option value="Alfa Romeo" <% if(currentTeam === 'Alfa Romeo') { %>selected<% } %>>Alfa Romeo</option>
                      <option value="Haas" <% if(currentTeam === 'Haas') { %>selected<% } %>>Haas</option>
                      <option value="Racing Point" <% if(currentTeam === 'Racing Point') { %>selected<% } %>>Racing Point</option>
                      <option value="Renault" <% if(currentTeam === 'Renault') { %>selected<% } %>>Renault</option>
                    </select>
                  </div>
                  <div class="col-md-4 mb-3">
                    <label for="raceSelect" class="form-label">Carrera</label>
                    <select id="raceSelect" name="race" class="form-select">
                      <% const currentRace = typeof selectedRace !== 'undefined' ? selectedRace : ''; %>
                      <option value="" <% if(currentRace === '') { %>selected<% } %>>Todas</option>
                      <option value="Monaco" <% if(currentRace === 'Monaco') { %>selected<% } %>>Gran Premio de Mónaco</option>
                      <option value="Silverstone" <% if(currentRace === 'Silverstone') { %>selected<% } %>>Gran Premio de Gran Bretaña</option>
                      <option value="Monza" <% if(currentRace === 'Monza') { %>selected<% } %>>Gran Premio de Italia</option>
                      <option value="Spa" <% if(currentRace === 'Spa') { %>selected<% } %>>Gran Premio de Bélgica</option>
                      <option value="Barcelona" <% if(currentRace === 'Barcelona') { %>selected<% } %>>Gran Premio de España</option>
                      <option value="Suzuka" <% if(currentRace === 'Suzuka') { %>selected<% } %>>Gran Premio de Japón</option>
                      <option value="Interlagos" <% if(currentRace === 'Interlagos') { %>selected<% } %>>Gran Premio de Brasil</option>
                      <option value="Singapore" <% if(currentRace === 'Singapore') { %>selected<% } %>>Gran Premio de Singapur</option>
                      <option value="Abu Dhabi" <% if(currentRace === 'Abu Dhabi') { %>selected<% } %>>Gran Premio de Abu Dhabi</option>
                      <option value="Melbourne" <% if(currentRace === 'Melbourne') { %>selected<% } %>>Gran Premio de Australia</option>
                      <option value="Austin" <% if(currentRace === 'Austin') { %>selected<% } %>>Gran Premio de Estados Unidos</option>
                      <option value="Mexico" <% if(currentRace === 'Mexico') { %>selected<% } %>>Gran Premio de México</option>
                      <option value="Montreal" <% if(currentRace === 'Montreal') { %>selected<% } %>>Gran Premio de Canadá</option>
                      <option value="Zandvoort" <% if(currentRace === 'Zandvoort') { %>selected<% } %>>Gran Premio de Países Bajos</option>
                      <option value="Imola" <% if(currentRace === 'Imola') { %>selected<% } %>>Gran Premio de Emilia-Romaña</option>
                    </select>
                  </div>
                </div>
                <div class="text-center">
                  <button type="submit" class="btn btn-danger">Buscar</button>
                </div>
              </form>
            </div>
          </section>

          <!-- Gallery Section -->
          <section class="gallery-list card bg-secondary text-light">
            <div class="card-body">
              <h2 class="card-title">Fotografías</h2>
              <% if (!inforGalleryPage || !inforGalleryPage.success || !inforGalleryPage.gallery || inforGalleryPage.gallery.length === 0) { %>
                <p class="error">No se encontraron fotografías</p>
              <% } else { %>
                <div class="row">
                  <% inforGalleryPage.gallery.forEach(function(image) { %>
                    <div class="col-md-4 col-sm-6 mb-4">
                      <div class="card h-100">
                        <img src="<%= image.url %>" class="card-img-top img-fluid" alt="<%= image.alt %>" style="height: 250px; object-fit: cover;">
                        <div class="card-footer bg-transparent border-0 text-center text-muted small pt-1">
                          <a href="<%= image.photographerUrl %>" target="_blank" class="text-muted text-decoration-none"><%= image.photographer %></a>
                        </div>
                      </div>
                    </div>
                  <% }); %>
                </div>
              <% } %>
            </div>
          </section>
        </div>
        <div class="col-12 text-center mt-3">
          <p class="lead mb-0">
            <a href="/logout" class="text-danger fw-bold">Cerrar sesión</a>
          </p>
        </div>
      </div>
    </div>
  </main>

  <footer class="py-5 text-center text-body-secondary bg-body-tertiary">
    <p>
      Blog template built for
      <a href="https://getbootstrap.com/">Bootstrap</a> by
      <a href="https://x.com/mdo">@mdo</a>.
    </p>
    <p class="mb-0"><a href="#">Back to top</a></p>
  </footer>

  <script>
    // Function to load dynamic filters based on year
    async function loadFiltersForYear(year) {
      const teamSelect = document.getElementById('teamSelect');
      const raceSelect = document.getElementById('raceSelect');
      
      if (!year) {
        return;
      }

      try {
        // Store current selections
        const currentTeam = teamSelect.value;
        const currentRace = raceSelect.value;

        // Fetch teams for selected year
        const teamsResponse = await fetch(`/api/teams/${year}`);
        const teams = await teamsResponse.json();
        
        // Fetch circuits for selected year
        const circuitsResponse = await fetch(`/api/circuits/${year}`);
        const circuitsData = await circuitsResponse.json();

        // Update teams dropdown
        if (Array.isArray(teams) && teams.length > 0) {
          teamSelect.innerHTML = '<option value="">Todos</option>';
          teams.forEach(team => {
            const option = document.createElement('option');
            option.value = team;
            option.textContent = team;
            option.selected = team === currentTeam;
            teamSelect.appendChild(option);
          });
        }

        // Update circuits dropdown
        if (circuitsData.success && circuitsData.circuits) {
          raceSelect.innerHTML = '<option value="">Todas</option>';
          circuitsData.circuits.forEach(circuit => {
            const option = document.createElement('option');
            option.value = circuit;
            option.textContent = circuit;
            option.selected = circuit === currentRace;
            raceSelect.appendChild(option);
          });
        }

      } catch (error) {
        console.error('Error fetching dynamic filters:', error);
      }
    }

    // Event listener for year change
    document.getElementById('yearSelect').addEventListener('change', async function() {
      await loadFiltersForYear(this.value);
    });

    // Load filters on page load with current year
    document.addEventListener('DOMContentLoaded', function() {
      const yearSelect = document.getElementById('yearSelect');
      if (yearSelect.value) {
        loadFiltersForYear(yearSelect.value);
      }
    });
  </script>
</body>
</html>